doctrine:
    dbal:
        driver: pdo_sqlite
        path: "%kernel.cache_dir%/user_test.db"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        entity_managers:
            default:
                auto_mapping: true
