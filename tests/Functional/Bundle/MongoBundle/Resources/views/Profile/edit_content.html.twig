{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('acme_mongo_profile_edit') }}" method="POST" class="fos_user_profile_edit">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
</form>
