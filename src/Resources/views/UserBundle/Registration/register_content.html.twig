{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'action': path(rollerworks_multi_user_user().getRoutePrefix() ~ '_registration_register'), 'attr': {'class': 'fos_user_registration_register'} }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
{{ form_end(form) }}
