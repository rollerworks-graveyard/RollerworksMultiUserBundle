{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path(rollerworks_multi_user_user().getRoutePrefix() ~ '_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
