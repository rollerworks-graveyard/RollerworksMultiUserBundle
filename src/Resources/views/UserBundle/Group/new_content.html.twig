{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'action': path(rollerworks_multi_user_user().getRoutePrefix() ~ '_group_new'), 'attr': {'class': 'fos_user_group_new'} }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.new.submit'|trans }}" />
    </div>
{{ form_end(form) }}
