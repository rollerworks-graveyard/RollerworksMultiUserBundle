<div class="fos_user_group_list">
    <ul>
    {% for group in groups %}
        <li><a href="{{ path(rollerworks_multi_user_user().getRoutePrefix() ~ '_group_show', {'groupName': group.getName()} ) }}">{{ group.getName() }}</a></li>
    {% endfor %}
    </ul>
</div>
